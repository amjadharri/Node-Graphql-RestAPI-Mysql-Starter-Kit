import {
	GraphQLList,
	GraphQLInt,
	GraphQLString,
	GraphQLNonNull
} from "graphql";
import {{controllerName name}}MainSchema from "./schema.js";
import DB from "./../../database/database.js";
import {{controllerName name}}Controller from "./Controller.js";
import Arguments from "./Arguments.js"


const {{controllerName name}}Mutations  = {
	{{controllerName name}}Update: {
		type: {{controllerName name}}MainSchema,
		args: {
			id: {
				type: GraphQLInt
			},
			...Arguments
		},
		async resolve(_, args) {
			return {{controllerName name}}Controller.{{controllerName name}}Update(_, args);
		}
	},
	{{controllerName name}}Create: {
		type: {{controllerName name}}MainSchema,
		args: Arguments,
		async resolve(_, args) {
			return {{controllerName name}}Controller.{{controllerName name}}Create(_, args);
		}
	},
	{{controllerName name}}Delete: {
		type: {{controllerName name}}MainSchema,
		args: {
			id: {
				type: new GraphQLNonNull(GraphQLInt)
			},
		},
		async resolve(_, args) {
			return {{controllerName name}}Controller.{{controllerName name}}Delete(_, args);
		}
	},
}


export default {{controllerName name}}Mutations;