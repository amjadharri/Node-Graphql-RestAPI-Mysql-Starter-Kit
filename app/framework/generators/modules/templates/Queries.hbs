import {
	GraphQLList,
	GraphQLInt,
	GraphQLString
} from "graphql";
import {{controllerName name}}MainSchema from "./Schema.js";
import {{controllerName name}}Controller from "./Controller.js"
import {QueryParams} from "./Arguments.js";


const {{controllerName name}}Queries = {
	{{controllerName name}}View: {
		type: {{controllerName name}}MainSchema,
		args: {
			id: {
				type: GraphQLInt
			},
			...QueryParams
		},
		async resolve(_, args) {
			return {{controllerName name}}Controller.{{controllerName name}}View(_, args);
		}

	},
	{{controllerName name}}ViewAll: {
		type: new GraphQLList({{controllerName name}}MainSchema),
		args: {
			id: {
				type: GraphQLInt
			},
			page: {
				type: GraphQLString
			},
			limit: {
				type: GraphQLString
			},
			...QueryParams
		},
		async resolve(_, args) {
			return {{controllerName name}}Controller.{{controllerName name}}ViewAll(_, args);
		}
	},
}

export default {{controllerName name}}Queries;